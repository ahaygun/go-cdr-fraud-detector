# Binaries & build output
/bin/
*.exe
*.out
*.test

# Local env
.env
.env.local

# OS / editor cruft
.DS_Store
.idea/
.vscode/

# Local working roadmap (kept out of the public repo for now)
PLAN.md

# Local wasm build artifacts (the committed demo lives in docs/, built via `make wasm`)
/web/main.wasm
/web/wasm_exec.js
